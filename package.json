{
  "name": "technodium-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && rm -rf dist public && cp -R frontend/dist dist && cp -R frontend/dist public"
  }
}
